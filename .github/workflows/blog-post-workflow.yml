name: Latest blog post workflow
on:
  schedule:
    # Runs every hour
    - cron: "0 * * * *"
jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "https://talktotheduck.dev/rss.xml, https://dev.to/feed/codename_one, https://medium.com/feed/@codename_one, https://lightrun.com/feed/, https://www.codenameone.com/feed.xml"
          max_post_count: 20
